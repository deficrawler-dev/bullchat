/**
 * Placeholder shape for a Room, pending the finalized rooms table
 * in DATABASE.md. Update this type once that schema is confirmed —
 * do not add fields speculatively.
 */
export interface Room {
  id: string;
  name: string;
  description: string;
  memberCount: number;
}